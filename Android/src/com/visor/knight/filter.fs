#pragma version(1)
#pragma rs java_package_name(com.visor.knight)

rs_allocation in;

uint32_t width;
uint32_t height;

uchar4 __attribute__((kernel)) root(uint32_t x, uint32_t y) {
   //lots of todos!
	return 0;
}
